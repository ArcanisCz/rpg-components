import React from 'react';
import {SampleComponent} from "moro-components";

const App = ({children}) => (
    <div>
        <h1>Apaap</h1>
        {children}
        <SampleComponent />
    </div>
);

export default App;
